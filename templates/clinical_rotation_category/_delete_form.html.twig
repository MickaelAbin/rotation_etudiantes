<form method="post" action="{{ path('app_clinical_rotation_category_delete', {'id': clinical_rotation_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ clinical_rotation_category.id) }}">
    <button class="btn py-2 px3 btn-danger rounded">Delete</button>
</form>
