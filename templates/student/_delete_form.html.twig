<form method="post" action="{{ path('app_student_delete', {'moodleUserID': student.moodleUserID}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ student.moodleUserID) }}">
    <button class="btn">Delete</button>
</form>
