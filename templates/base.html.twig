<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
        <title>{% block title %}Mes Gardes{% endblock %}</title>
        <!-- Favicon Intranet -->
        <link rel="icon" href="https://connect.oniris-nantes.fr/theme/image.php/lambda/theme/1680610928/favicon">
        <!-- Font Awesome 6.4.0 (Free) -->
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
        <!-- Google fonts -->
        <link href="https://fonts.googleapis.com/css?family=Raleway:400%7CRaleway:500" rel="stylesheet">
        <!-- Bootstrap 5.2.3 CSS -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">

        {% block stylesheets %}
        {% endblock %}

    </head>
    <body>
        <header class="d-flex justify-content-center">
            <div class="col-10">
                <img src="{{ asset('assets/oniris_banner.jpg') }}">
            </div>
            <div>

            </div>


        </header>
        <main>
            <div class="m-auto">
                <div class="container">
                    <div class="row gx-4 gx-lg-5 justify-content-center">
                        <h1 class="mx-auto text-center p-2">Gardes étudiantes</h1>
                    </div>
                </div>
                <div class="container py-2 px-4 d-flex align-items-center justify-content-center position-static">
                    {% for group,messages in app.flashes %}
                        {% for message in messages %}
                            <div class="alert alert-{{ group }} mt-2 col-7 text-center" role="alert">{{ message }}</div>
                        {% endfor %}
                    {% endfor %}
                </div>
             </div>
            <div class="d-flex justify-content-center">
                <nav class="nav nav-tabs flex-column flex-sm-row col-10" id="navbar-top">
                    <a class="flex-sm-fill text-sm-center nav-link" href="{{ path('home') }}">Accueil</a>
                    <a class="flex-sm-fill text-sm-center nav-link active" aria-current="page" href="#">Liste par date</a>
                    <a class="flex-sm-fill text-sm-center nav-link" href="#">Calendrier</a>
                    <a class="flex-sm-fill text-sm-center nav-link" href="#">Liste par étudiants</a>
                    <a class="flex-sm-fill text-sm-center nav-link" href="#">Historique des échanges</a>
                </nav>
            </div>
            <section class="d-flex justify-content-center">
                <div class="bg-light p-2 col-10">

                    {% block main %}
                    {% endblock %}

                </div>
            </section>
        </main>

        <footer class="bg-dark small text-center text-white-50 p-4">
            <div class="container p-2">&copy; Oniris, Nantes | Pôle Applicatif 2023</div>
        </footer>

        <!-- Bootstrap 5.2.3 JS -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
        <script src = {{ asset('js/base.js') }}></script>

        {% block javascripts %}
        {% endblock %}

    </body>
</html>
