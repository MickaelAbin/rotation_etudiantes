<form method="post" action="{{ path('app_enrolment_delete', {'id': enrolment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enrolment.id) }}">
    <button class="btn">Delete</button>
</form>
