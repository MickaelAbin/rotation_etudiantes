<form method="post" action="{{ path('app_university_calendar_delete', {'id': university_calendar.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ university_calendar.id) }}">
    <button class="btn">Delete</button>
</form>
